apiVersion: v1
kind: Service
metadata:
  name: oficina-cadastro
  namespace: oficina
  labels:
    app.kubernetes.io/name: oficina-cadastro
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: oficina-cadastro
    app.kubernetes.io/component: runtime
  ports:
    - name: http
      port: 8080
      targetPort: http
